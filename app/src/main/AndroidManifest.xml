<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aca.kktrijumf">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_pun"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_pun"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SelectPlayerSmsActivity"></activity>
        <activity android:name=".AddCoachActivity" />
        <activity android:name=".VidiPlacanjaActivity" />
        <activity android:name=".DodajPlacanjeActivity" />
        <activity android:name=".ListPlayerActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".EditUserActivity" />
        <activity android:name=".AddUser" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".NotificationReceiver" />
    </application>

    <uses-permission android:name="android.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.SEND_SMS"/>

</manifest>